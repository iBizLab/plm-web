import { createShape } from './util.mjs';

const Ellipse = createShape('ellipse', {
    attrs: {
        body: {
            refCx: '50%',
            refCy: '50%',
            refRx: '50%',
            refRy: '50%',
        },
    },
});

export { Ellipse };
