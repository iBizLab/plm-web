import { Registry } from '../registry.mjs';
import * as main from './main.mjs';

var EdgeAnchor;
(function (EdgeAnchor) {
    EdgeAnchor.presets = main;
    EdgeAnchor.registry = Registry.create({
        type: 'edge endpoint',
    });
    EdgeAnchor.registry.register(EdgeAnchor.presets, true);
})(EdgeAnchor || (EdgeAnchor = {}));

export { EdgeAnchor };
