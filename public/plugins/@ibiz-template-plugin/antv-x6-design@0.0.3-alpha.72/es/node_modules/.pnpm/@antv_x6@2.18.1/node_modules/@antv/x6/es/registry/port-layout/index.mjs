import { Registry } from '../registry.mjs';
import * as main from './main.mjs';

var PortLayout;
(function (PortLayout) {
    PortLayout.presets = main;
    PortLayout.registry = Registry.create({
        type: 'port layout',
    });
    PortLayout.registry.register(PortLayout.presets, true);
})(PortLayout || (PortLayout = {}));

export { PortLayout };
