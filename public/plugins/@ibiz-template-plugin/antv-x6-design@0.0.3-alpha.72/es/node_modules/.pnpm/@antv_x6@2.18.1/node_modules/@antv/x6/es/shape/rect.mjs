import { createShape } from './util.mjs';

const Rect = createShape('rect', {
    attrs: {
        body: {
            refWidth: '100%',
            refHeight: '100%',
        },
    },
});

export { Rect };
