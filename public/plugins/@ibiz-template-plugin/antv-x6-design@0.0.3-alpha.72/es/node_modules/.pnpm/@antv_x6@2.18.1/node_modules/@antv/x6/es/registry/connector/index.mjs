import { Registry } from '../registry.mjs';
import * as main from './main.mjs';

var Connector;
(function (Connector) {
    Connector.presets = main;
    Connector.registry = Registry.create({
        type: 'connector',
    });
    Connector.registry.register(Connector.presets, true);
})(Connector || (Connector = {}));

export { Connector };
