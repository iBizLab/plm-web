import { Basecoat } from './basecoat.mjs';

/* eslint-disable no-underscore-dangle */
class Disablable extends Basecoat {
    get disabled() {
        return this._disabled === true;
    }
    enable() {
        delete this._disabled;
    }
    disable() {
        this._disabled = true;
    }
}

export { Disablable };
