import { createSensor as createSensor$2 } from './object.mjs';
import { createSensor as createSensor$1 } from './observer.mjs';

const createSensor = typeof ResizeObserver !== 'undefined'
    ? createSensor$1
    : createSensor$2;

export { createSensor };
